<button class="glowing-button">BUY TICKETS HERE!</button>

<style>
  *,
  *::before,
  *::after {
    box-sizing: border-box;
  }

  /* h1 {
    color: #49e819;
    font-family: "Montserrat", sans-serif;
    text-align: center;
  } */

  .glowing-button-wrap {
    text-align: center;
    position: absolute;
    left: 50%;
    top: 50%;
    transform: translate(-50%, -50%);
    max-width: 800px;
  }

  .glowing-button {
    white-space: nowrap;
    margin: 10px;
    position: relative;
    background-color: var(--secondary);
    box-shadow: 0 0 20px 0 var(--secondary);
    border: 2px solid var(--secondary);
    padding: 20px 80px;
    background-color: var(--primary);
    transition: 0.4s;
    font-size: 20px;
    cursor: pointer;
    color: var(--primary);
    z-index: 0;
    transform-style: preserve-3d;
  }

  .glowing-button::before {
    content: "";
    width: 100%;
    height: calc(50% + 1px);
    background-color: var(--secondary);
    position: absolute;
    left: 0;
    top: 0;
    border: 2px solid var(--secondary);
    transition: 0.4s;
    box-shadow: 0 0 20px 0 var(--secondary);
    z-index: -1;
  }

  .glowing-button::after {
    content: "";
    width: 100%;
    height: calc(50% + 1px);
    background-color: var(--secondary);
    position: absolute;
    right: 0;
    bottom: 0;
    border: 2px solid var(--secondary);
    transition: 0.4s;
    box-shadow: 0 0 20px 0 var(--secondary);
    z-index: -1;
  }

  .glowing-button:hover {
    color: var(--secondary);
  }

  .glowing-button:hover::before {
    width: 30px;
    height: 10px;
    top: -5px;
    left: -5px;
    transform: skewX(30deg);
    background-color: var(--primary);
  }

  .glowing-button:hover::after {
    width: 30px;
    height: 10px;
    bottom: -5px;
    right: -5px;
    transform: skewX(-30deg);
    background-color: var(--primary);
  }
</style>
